@page
@model Detector.Pages.LogsModel
@{
    ViewData["Title"] = "Logs do Sistema";
}

<div class="d-flex align-items-center gap-2 mb-4">
    <i class="fa-solid fa-file-lines fa-2x text-secondary"></i>
    <div>
        <h1 class="fw-bold mb-0">Logs do Sistema</h1>
        <p class="text-muted mb-0 small">Histórico de requisições, respostas e feedbacks — armazenados no banco de dados</p>
    </div>
</div>

<ul class="nav nav-tabs mb-3" id="logTabs" role="tablist">
    <li class="nav-item" role="presentation">
        <button class="nav-link active" id="req-tab" data-bs-toggle="tab" data-bs-target="#req"
                type="button" role="tab">
            <i class="fa-solid fa-arrow-right-to-bracket me-1"></i>Requisições
        </button>
    </li>
    <li class="nav-item" role="presentation">
        <button class="nav-link" id="resp-tab" data-bs-toggle="tab" data-bs-target="#resp"
                type="button" role="tab">
            <i class="fa-solid fa-arrows-left-right me-1"></i>Requisições + Respostas
        </button>
    </li>
    <li class="nav-item" role="presentation">
        <button class="nav-link" id="feed-tab" data-bs-toggle="tab" data-bs-target="#feed"
                type="button" role="tab">
            <i class="fa-solid fa-star me-1"></i>Feedbacks
        </button>
    </li>
</ul>

<div class="tab-content">
    <div class="tab-pane fade show active" id="req" role="tabpanel">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
                <span><i class="fa-solid fa-database me-1"></i>Tabela: LogsRequisicao</span>
                <span class="badge bg-secondary">@Model.LogRequisicao.Count registros</span>
            </div>
            <div class="card-body p-0">
                @if (Model.LogRequisicao.Count == 0)
                {
                    <p class="text-muted text-center py-4">Nenhum log encontrado ainda.</p>
                }
                else
                {
                    <div class="log-area">
                        @foreach (var item in Model.LogRequisicao)
                        {
                            <div class="log-linha">@item.DataHora.ToString("dd/MM/yyyy HH:mm:ss") — @item.Input</div>
                        }
                    </div>
                }
            </div>
        </div>
    </div>

    <div class="tab-pane fade" id="resp" role="tabpanel">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
                <span><i class="fa-solid fa-database me-1"></i>Tabela: LogsRequisicaoResposta</span>
                <span class="badge bg-secondary">@Model.LogRequisicaoResposta.Count registros</span>
            </div>
            <div class="card-body p-0">
                @if (Model.LogRequisicaoResposta.Count == 0)
                {
                    <p class="text-muted text-center py-4">Nenhum log encontrado ainda.</p>
                }
                else
                {
                    <div class="log-area">
                        @foreach (var item in Model.LogRequisicaoResposta)
                        {
                            <div class="log-linha">@item.DataHora.ToString("dd/MM/yyyy HH:mm:ss") — @item.Input — @item.Resposta</div>
                        }
                    </div>
                }
            </div>
        </div>
    </div>

    <div class="tab-pane fade" id="feed" role="tabpanel">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
                <span><i class="fa-solid fa-database me-1"></i>Tabela: LogsFeedback</span>
                <span class="badge bg-secondary">@Model.LogFeedback.Count registros</span>
            </div>
            <div class="card-body p-0">
                @if (Model.LogFeedback.Count == 0)
                {
                    <p class="text-muted text-center py-4">Nenhum feedback registrado ainda.</p>
                }
                else
                {
                    <div class="log-area">
                        @foreach (var item in Model.LogFeedback)
                        {
                            var isPos = item.TipoFeedback == Detector.Enums.EnumTipoFeedback.Positivo;
                            var isNeg = item.TipoFeedback == Detector.Enums.EnumTipoFeedback.Negativo;
                            <div class="log-linha @(isPos ? "log-positivo" : isNeg ? "log-negativo" : "")">
                                @item.DataHora.ToString("dd/MM/yyyy HH:mm:ss") — @item.TipoFeedback — @item.Input — @item.RespostaIA
                            </div>
                        }
                    </div>
                }
            </div>
        </div>
    </div>
</div>
